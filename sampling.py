import torch

# G equals sum sortie lindecoder et defdecoder?


def local_sampler(p, S=torch.normal(1, 0.5, size=(1, 64, 192, 192)), G):

    return None


def sampler(p, S, G):
    # for p in pixel of S
    # W(S,G)(p) = local_sampler(p)
    # return W(S,G)
    return None
